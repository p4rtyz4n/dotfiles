[core]
  excludesFile = ~/.config/git/ignore

[alias]
  merge-branch = merge --log --no-commit --no-ff
  fix-submodules = submodule update --recursive --init
  recursive = "!sh -c 'for i in . `git submodule foreach -q --recursive pwd`; do cd $i && git $@; done' -"
  recursive-sh = "!sh -c 'for i in . `git submodule foreach -q --recursive pwd`; do cd $i && $@; done' -"

[branch]
 sort = -committerdate

[color]
	diff = auto
	status = auto
	branch = auto

[commit]
	gpgsign = true

[diff]
  tool = icdiff

[difftool]
  prompt = false

[difftool "icdiff"]
  cmd = /usr/local/bin/icdiff --line-numbers $LOCAL $REMOTE

[filter "lfs"]
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
	required = true

[merge]
	tool = vimdiff

[pull]
	rebase = true

[push]
  followTags = true

[rerere]
	enabled = true

[user]
	name = Bohdan Pavlov 
	email = bohpavlov@gmail.com
	signingkey = 0x95091F9A30FB2800

[commit]
	gpgsign = true
